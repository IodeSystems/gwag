syntax = "proto3";

package greeter.v1;

option go_package = "github.com/iodesystems/go-api-gateway/examples/multi/gen/greeter/v1;greeterv1";

service GreeterService {
  rpc Hello(HelloRequest) returns (HelloResponse);

  // Server-streaming → GraphQL subscription.
  // The request fields become subscription arguments; the stream
  // message becomes the event payload.
  rpc Greetings(GreetingsFilter) returns (stream Greeting);

  // Client-streaming and bidi are filtered with a warning.
  rpc Echo(stream HelloRequest) returns (stream HelloResponse);
}

message HelloRequest {
  string name = 1;
}

message HelloResponse {
  string greeting = 1;
}

message GreetingsFilter {
  // Filter by recipient name. "*" or empty matches all.
  string name = 1;
}

message Greeting {
  string greeting = 1;
  string for_name = 2;
}
