// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v3.21.12
// source: gw/proto/quotaauth/v1/quotaauth.proto

package quotaauthv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	QuotaAuthorizer_AcquireBlock_FullMethodName = "/gateway.quotaauth.v1.QuotaAuthorizer/AcquireBlock"
)

// QuotaAuthorizerClient is the client API for QuotaAuthorizer service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// QuotaAuthorizer is the optional delegate the gateway calls to grant
// blocks of permits per caller. Implement it in your quota service and
// register it under namespace "_quota_auth" / version v1.
//
// Wire flow: the gateway gates dispatch on a local permit bucket keyed
// by (caller_id, namespace, version). When a bucket is empty (or close
// to it) the gateway calls AcquireBlock with a requested permit count;
// the delegate replies with granted_permits + valid_until. Permits are
// debited locally per dispatch; no per-request RPC on the hot path.
//
// Code policy mirrors CallerAuthorizer / AdminAuthorizer:
//   - OK: granted_permits is the block size; valid_until bounds the
//     staleness window so the delegate can enforce time-windowed
//     quotas (per-second, per-minute) without per-request consult.
//   - DENIED: caller exhausted its quota; gateway surfaces 429 + a
//     Retry-After hint derived from valid_until (when set).
//   - UNAVAILABLE / NOT_CONFIGURED / UNSPECIFIED / transport error:
//     fail-open — gateway grants a small emergency block and retries
//     on the next refill. Matches gw's default-allow posture; flipped
//     to fail-closed once WithQuotaEnforce ships (plan §Caller-ID +
//     quota ladder).
type QuotaAuthorizerClient interface {
	AcquireBlock(ctx context.Context, in *AcquireBlockRequest, opts ...grpc.CallOption) (*AcquireBlockResponse, error)
}

type quotaAuthorizerClient struct {
	cc grpc.ClientConnInterface
}

func NewQuotaAuthorizerClient(cc grpc.ClientConnInterface) QuotaAuthorizerClient {
	return &quotaAuthorizerClient{cc}
}

func (c *quotaAuthorizerClient) AcquireBlock(ctx context.Context, in *AcquireBlockRequest, opts ...grpc.CallOption) (*AcquireBlockResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(AcquireBlockResponse)
	err := c.cc.Invoke(ctx, QuotaAuthorizer_AcquireBlock_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// QuotaAuthorizerServer is the server API for QuotaAuthorizer service.
// All implementations must embed UnimplementedQuotaAuthorizerServer
// for forward compatibility.
//
// QuotaAuthorizer is the optional delegate the gateway calls to grant
// blocks of permits per caller. Implement it in your quota service and
// register it under namespace "_quota_auth" / version v1.
//
// Wire flow: the gateway gates dispatch on a local permit bucket keyed
// by (caller_id, namespace, version). When a bucket is empty (or close
// to it) the gateway calls AcquireBlock with a requested permit count;
// the delegate replies with granted_permits + valid_until. Permits are
// debited locally per dispatch; no per-request RPC on the hot path.
//
// Code policy mirrors CallerAuthorizer / AdminAuthorizer:
//   - OK: granted_permits is the block size; valid_until bounds the
//     staleness window so the delegate can enforce time-windowed
//     quotas (per-second, per-minute) without per-request consult.
//   - DENIED: caller exhausted its quota; gateway surfaces 429 + a
//     Retry-After hint derived from valid_until (when set).
//   - UNAVAILABLE / NOT_CONFIGURED / UNSPECIFIED / transport error:
//     fail-open — gateway grants a small emergency block and retries
//     on the next refill. Matches gw's default-allow posture; flipped
//     to fail-closed once WithQuotaEnforce ships (plan §Caller-ID +
//     quota ladder).
type QuotaAuthorizerServer interface {
	AcquireBlock(context.Context, *AcquireBlockRequest) (*AcquireBlockResponse, error)
	mustEmbedUnimplementedQuotaAuthorizerServer()
}

// UnimplementedQuotaAuthorizerServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedQuotaAuthorizerServer struct{}

func (UnimplementedQuotaAuthorizerServer) AcquireBlock(context.Context, *AcquireBlockRequest) (*AcquireBlockResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method AcquireBlock not implemented")
}
func (UnimplementedQuotaAuthorizerServer) mustEmbedUnimplementedQuotaAuthorizerServer() {}
func (UnimplementedQuotaAuthorizerServer) testEmbeddedByValue()                         {}

// UnsafeQuotaAuthorizerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to QuotaAuthorizerServer will
// result in compilation errors.
type UnsafeQuotaAuthorizerServer interface {
	mustEmbedUnimplementedQuotaAuthorizerServer()
}

func RegisterQuotaAuthorizerServer(s grpc.ServiceRegistrar, srv QuotaAuthorizerServer) {
	// If the following call panics, it indicates UnimplementedQuotaAuthorizerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&QuotaAuthorizer_ServiceDesc, srv)
}

func _QuotaAuthorizer_AcquireBlock_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(AcquireBlockRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(QuotaAuthorizerServer).AcquireBlock(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: QuotaAuthorizer_AcquireBlock_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(QuotaAuthorizerServer).AcquireBlock(ctx, req.(*AcquireBlockRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// QuotaAuthorizer_ServiceDesc is the grpc.ServiceDesc for QuotaAuthorizer service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var QuotaAuthorizer_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "gateway.quotaauth.v1.QuotaAuthorizer",
	HandlerType: (*QuotaAuthorizerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "AcquireBlock",
			Handler:    _QuotaAuthorizer_AcquireBlock_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "gw/proto/quotaauth/v1/quotaauth.proto",
}
